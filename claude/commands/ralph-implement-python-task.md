---
name: ralph-implement-python-task
description: Autonomous Python task implementation (requires approved plan from ralph-plan-task)
arguments:
  - name: task_ref
    description: Task reference in format "project#N" (e.g., "myproject#18")
    required: true
---

Ты АВТОНОМНО выполняешь реализацию Python-задачи. Один запуск = одна задача.

## 🚨 PRODUCTION QUALITY — НЕ MVP!

**Это PRODUCTION код, а не MVP/прототип.**

### Обязательные требования

1. **ВСЕ 12 ФАЗ должны быть выполнены** — пропуск этапов ЗАПРЕЩЁН
2. **ВСЕ тесты должны проходить** — не "большинство", а ВСЕ
3. **ВСЕ замечания от ревью должны быть обработаны** — каждое с отметкой `[main-claude]: Fixed/Deferred/Declined`
4. **ВСЕ ошибки линтеров должны быть исправлены** — код должен быть чистым
5. **Качество кода** — следуй паттернам проекта, не срезай углы

### ЗАПРЕЩЕНО

- ❌ Пропускать фазы "для скорости"
- ❌ Оставлять failing tests "на потом"
- ❌ **Помечать тесты как `@pytest.mark.skip` чтобы обойти падающие тесты**
- ❌ Игнорировать замечания ревью
- ❌ Писать TODO/FIXME вместо реализации
- ❌ Упрощать реализацию относительно плана без согласования
- ❌ Считать задачу завершённой если что-то не работает

### 🚨 КРИТИЧНО: Skipped tests = FAILED TASK

**Stop hook заблокирует завершение если обнаружит skipped тесты!**

**ЗАПРЕЩЁННЫЕ паттерны** (hook их детектирует):
```
❌ "7 tests skipped"
❌ "marked for follow-up debugging"
❌ "@pytest.mark.skip"
❌ "2 passed, 5 skipped"
```

**Что делать если тест падает:**
1. **ИСПРАВЬ тест** — потрать время на отладку
2. Если не можешь исправить → **hold + blocks**
3. НЕ помечай как skip

**`@pytest.mark.skip` разрешён ТОЛЬКО если:**
- Тест был skip ДО начала текущей задачи
- Тест требует внешней инфраструктуры (CI, staging)

**`@pytest.mark.skip` ЗАПРЕЩЁН для:**
- Новых тестов, которые не проходят
- "Сложных" тестов, которые не хочется отлаживать
- Тестов "для follow-up работы"

### Если не можешь выполнить качественно

**НЕ ДЕЛАЙ ПЛОХО** — лучше поставь задачу в `hold` с описанием проблемы, чем сдать некачественную работу.

## ⚠️ ПРИНЦИПЫ АВТОНОМНОСТИ

- **Полная автономность** — НИКАКИХ вопросов к человеку
- **План обязателен** — без секции `## Plan` в задаче не начинай
- **При блокировке → hold** — записать проблему в `## Blocks` и выйти
- **Один запуск = одна задача** — после завершения/блокировки выходи

## ⚠️ ЗАПРЕЩЕНО

- Использовать `AskUserQuestion`
- Использовать фразу `need feedback`
- Ждать ответа пользователя
- Продолжать при блокировке
- Заменять недоступные инструменты своим анализом

## ⚠️ ТРЕБОВАНИЯ К МОДЕЛИ

**Используй Opus 4.5** — это обеспечивает максимальное качество анализа и кода.

## Используемые инструменты

| Инструмент | Фаза | Контекст |
|------------|------|----------|
| `md-task-mcp` | 0, 1, 11 | основной |
| `TodoWrite` | 0-11 | основной (отслеживание прогресса) |
| `Read` | 2 | основной (чтение файлов из Scope) |
| `context7`, `pyright-lsp`, `playwright` | 3 | основной |
| UI Review (playwright + Opus) | 5 | **изолированный** (Task) |
| `ralph review` | 6 | **изолированные** (CLI, 4 команды) |
| `Bash` (chmod, git) | 9, 11 | основной (cleanup, commit) |

## Workflow

0. Validate Task (план обязателен)
1. Update Task
2. Read Plan Context (файлы из Scope)
3. Implementation
4. Initial Testing
5. **UI Review** (визуальный анализ скриншотов — если есть frontend)
6. **Reviews** (ralph review — изолированные контексты)
7. Final Testing (+ финальная UI проверка)
8. Linters
9. Cleanup
10. Documentation
11. Complete (коммит + отчёт в задачу)

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Автономное выполнение

**НЕ ОСТАНАВЛИВАЙСЯ** между этапами workflow. Выполняй все фазы последовательно без пауз.

### Обработка блокировок

При ЛЮБОЙ ситуации, требующей человека — **закоммитить работу, записать в blocks и выйти**:

```bash
# 1. Сохранить текущую работу (если есть изменения)
git add -A
git commit -m "WIP: {project}#{number} - blocked: {краткое описание}"
```

```python
# 2. Записать блокировку в задачу (используй поле blocks!)
blocks_text = """
- [{YYYY-MM-DD HH:MM}] {Краткое описание проблемы}
  - Что пытался сделать: {описание действий}
  - Почему не получилось: {причина блокировки}
  - Что нужно от человека: {конкретный запрос}
  - Коммит с незавершённой работой: {commit_hash}
"""

update_task(
    project="<project>",
    number=<N>,
    status="hold",
    blocks=blocks_text  # ← поле blocks, НЕ body!
)
```

**⚠️ ВАЖНО**: Коммит перед hold обязателен — иначе незавершённая работа будет мешать следующим задачам в loop.

**После записи блокировки — ВЫЙТИ. Не пытаться продолжать.**

### Типичные блокировки

- Отсутствует секция `## Plan` в задаче
- Непонятные или противоречивые требования в плане
- Security/PR/Codex review недоступен
- Неразрешимый конфликт с существующим кодом
- Требуется архитектурное решение

### ⚠️ ОБЯЗАТЕЛЬНО: CLAUDE.md — читай ДО проблем, пиши ПОСЛЕ решения

**Phase 2 ТРЕБУЕТ** прочитать CLAUDE.md ДО начала работы — не жди проблем!

**Если всё же столкнулся с проблемой:**

1. **СНАЧАЛА** поищи решение в CLAUDE.md:
```bash
# Поиск по ключевому слову
grep -ri "<ключевое слово>" /workspace/CLAUDE.md /workspace/services/*/CLAUDE.md 2>/dev/null
```

2. **Если решения нет** — реши проблему и **ЗАПИШИ в Phase 10**

**⚠️ ЗАПРЕЩЕНО:**
- Угадывать синтаксис команд (>2 попыток = читай документацию!)
- Перебирать хосты/порты (они задокументированы!)
- Решить проблему и НЕ записать решение

**НЕ ТРАТЬ ВРЕМЯ** на отладку того, что уже описано в документации!

### Обнаружение проблем в коде

При обнаружении проблем **не относящихся к текущей задаче** (баги, технический долг, уязвимости):

1. **НЕ исправляй** проблему в рамках текущей задачи
2. **Создай новую задачу** только с описанием проблемы:

```python
create_task(
    project="<project>",
    description="<Краткое описание проблемы>",
    body="""
### Проблема
<Что обнаружено>

### Где найдено
- Файл: `path/to/file.py`
- Строки: N-M

### Контекст обнаружения
Найдено при выполнении задачи #{current_task_number}
"""
)
```

3. **Продолжай текущую задачу** — новая задача будет обработана отдельно

## Phase 0: Validate Task (ОБЯЗАТЕЛЬНО)

**Task reference:** `$ARGUMENTS`

### Шаг 1: Получи задачу

```python
task = get_task(project="<project>", number=<N>)
```

### Шаг 2: Проверь наличие плана

Задача ДОЛЖНА содержать план в поле `plan` (не в body!).

```python
task = get_task(project="<project>", number=<N>)
plan = task.get("plan", "")
if not plan:
    # БЛОКИРОВКА — плана нет
```

**Если плана нет — БЛОКИРОВКА:**

```python
update_task(
    project="<project>",
    number=<N>,
    status="hold",
    blocks="""
- [{timestamp}] Отсутствует план реализации
  - Что пытался сделать: начать реализацию задачи
  - Почему не получилось: задача не содержит плана в поле plan
  - Что нужно от человека: выполнить /ralph-plan-task {project}#{number}
"""
)
```

**После записи блокировки — ВЫЙТИ.**

### Шаг 3: Создай todo list

```
TodoWrite([
  { content: "Phase 0: Validate Task", status: "completed", activeForm: "Validating task" },
  { content: "Phase 1: Update Task", status: "in_progress", activeForm: "Updating task status" },
  { content: "Phase 2: Read Plan Context", status: "pending", activeForm: "Reading files from Scope" },
  { content: "Phase 3: Implementation", status: "pending", activeForm: "Implementing feature" },
  { content: "Phase 4: Initial Testing", status: "pending", activeForm: "Running initial tests" },
  { content: "Phase 5: UI Review", status: "pending", activeForm: "Running UI review" },
  { content: "Phase 6: Reviews", status: "pending", activeForm: "Running reviews (isolated)" },
  { content: "Phase 7: Final Testing", status: "pending", activeForm: "Running final tests" },
  { content: "Phase 8: Linters", status: "pending", activeForm: "Running linters" },
  { content: "Phase 9: Cleanup", status: "pending", activeForm: "Cleaning up" },
  { content: "Phase 10: Documentation", status: "pending", activeForm: "Updating documentation" },
  { content: "Phase 11: Complete Task", status: "pending", activeForm: "Committing and completing" }
])
```

**ОБНОВЛЯЙ TODO LIST** при переходе между фазами (0-11):
- Текущая фаза → `in_progress`
- Завершённая фаза → `completed`

## Phase 1: Update Task

**Проверь текущий статус** — задача может уже быть в "work" после планирования. Если уже "work" — просто продолжай. Если нет — обнови статус на "work".

## Phase 2: Read Plan Context

**План содержит основную информацию**, но ты работаешь в отдельном контексте от агента планирования.

### Шаг 1: ОБЯЗАТЕЛЬНО прочитай CLAUDE.md (ПЕРЕД любой работой!)

**⚠️ СТОП! Сначала определи затрагиваемые сервисы из Scope в плане.**

Например, если Scope содержит `services/web/templates/employees.html`, то сервис = `web`.

**Теперь ОБЯЗАТЕЛЬНО вызови Read tool для КАЖДОГО файла:**

1. `Read("/workspace/CLAUDE.md")` — **ВСЕГДА**
2. `Read("/workspace/services/<service>/CLAUDE.md")` — для каждого сервиса из Scope
3. `Read("/workspace/services/<service>/tests/CLAUDE.md")` — если будут тесты

**Пример для задачи с Scope `services/web/templates/...`:**
```
Read("/workspace/CLAUDE.md")
Read("/workspace/services/web/CLAUDE.md")
Read("/workspace/services/web/tests/CLAUDE.md")
```

**⚠️ ЭТО НЕ ОПЦИОНАЛЬНО!** Там находятся:
- Точный синтаксис запуска тестов
- Правильные хосты/порты для Playwright
- Решения известных проблем

**🚫 ЗАПРЕЩЕНО:**
- Читать только корневой CLAUDE.md
- Пропускать CLAUDE.md сервиса
- Угадывать синтаксис команд вместо чтения документации

### Шаг 2: Прочитай файлы из плана

- Найди список файлов в плане (секция `### Scope` или аналогичная)
- Открой каждый указанный файл
- Если есть `### Code Context` — используй для понимания паттернов
- При необходимости читай дополнительные связанные файлы

**Цель**: получить актуальное содержимое файлов и полный контекст для реализации.

### ⚠️ ОБЯЗАННОСТЬ: Записывай решения проблем

**Если во время работы ты:**
- Потратил >2 попытки на решение проблемы
- Нашёл неочевидный синтаксис или API
- Обнаружил особенность инфраструктуры

**→ ТЫ ОБЯЗАН записать решение в CLAUDE.md в Phase 10!**

Это критически важно для следующих запусков — не трать время других агентов на уже решённые проблемы.

## Phase 3: Implementation

Реализуй по плану. Следуй паттернам и стилю текущего репозитория.

Доступные инструменты:
- **context7** — документация библиотек
- **LSP** — статический анализ Python (типы, определения, ссылки)
- **Playwright MCP** — тестирование UI

## Phase 4: Initial Testing

**После реализации** создай и запусти тесты. Следуй паттернам существующих тестов.

### Обязательные типы тестов

#### 1. Unit Tests
- Тесты отдельных функций и методов
- Моки для внешних зависимостей
- Покрытие основных сценариев (happy path)
- Покрытие edge cases (граничные случаи, пустые данные, None, ошибки)

#### 2. API Tests (если есть endpoints)
- Тесты HTTP endpoints (GET, POST, PUT, DELETE)
- Проверка response codes (200, 400, 401, 403, 404, 500)
- Валидация request/response schema
- Тесты с авторизацией и без

#### 3. UI Tests (ОБЯЗАТЕЛЬНО если есть frontend)

**⚠️ UI тестирование через Playwright MCP обязательно!**

**⚠️ ОБЯЗАТЕЛЬНО используй `data-testid` для всех тестируемых элементов!**

```html
<!-- В HTML/шаблонах добавляй data-testid -->
<button data-testid="submit-button">Submit</button>
<input data-testid="email-input" type="email" />
<div data-testid="user-profile">...</div>
```

```python
# В тестах используй data-testid для надёжных селекторов
page.click('[data-testid="submit-button"]')
page.fill('[data-testid="email-input"]', 'test@example.com')
expect(page.locator('[data-testid="user-profile"]')).to_be_visible()
```

**Преимущества data-testid:**
- Устойчивость к изменениям CSS классов и структуры
- Явное обозначение тестируемых элементов
- Не зависит от текста (локализация)

Используй Playwright MCP для визуальной проверки:

```
1. Открой страницу с изменениями
2. Сделай скриншот (screenshot)
3. Проверь визуально:
   - Нет перекрытия элементов
   - Корректное выравнивание
   - Читаемость текста
   - Отображение на разных разрешениях
4. Проверь интерактивность:
   - Кликабельность кнопок
   - Работу форм
   - Навигацию
```

**Чеклист UI тестирования:**
- [ ] Скриншот основного состояния страницы
- [ ] Проверка на перекрытие элементов
- [ ] Проверка форм и валидации
- [ ] Проверка навигации между страницами
- [ ] Скриншот ошибочных состояний (если применимо)

### Логирование тестов

**Логи тестов пиши в `~/.claude/logs/tests/`:**

```bash
# Создай директорию если нет
mkdir -p ~/.claude/logs/tests

# Запуск тестов с сохранением логов
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
./run_tests.sh path/to/tests 2>&1 | tee ~/.claude/logs/tests/${PROJECT}_${TASK_NUM}_${TIMESTAMP}.log
```

**НЕ используй `/tmp/` для логов** — они не будут видны в `ralph logs`.

### Чеклист тестов

- [ ] **Happy path**: основной сценарий работает
- [ ] **Edge cases**: пустые данные, граничные значения
- [ ] **Error handling**: ошибки обрабатываются корректно
- [ ] **Permissions**: права доступа проверены
- [ ] **Existing tests**: старые тесты не сломаны

**Все тесты должны проходить** перед переходом к UI Review.

## Phase 5: UI Review (если есть frontend)

**АКТИВНОЕ функциональное тестирование UI** — не просто скриншоты!

### ⚠️ КРИТИЧНО: Это полноценное QA тестирование

**ЗАПРЕЩЕНО:**
- ❌ Просто открыть страницу и сделать скриншот
- ❌ Написать "UI выглядит корректно" без активного тестирования
- ❌ Пропустить тестирование интерактивных элементов

**ОБЯЗАТЕЛЬНО:**
- ✅ Прокликать ВЕСЬ функционал
- ✅ Заполнить ВСЕ формы разными данными
- ✅ Попытаться СЛОМАТЬ приложение
- ✅ Проверить edge cases и граничные значения

### Шаг 1: Активное функциональное тестирование через Opus

Запусти АКТИВНЫЙ UI Review через Task tool с Opus:

```
Task(
  subagent_type="general-purpose",
  prompt="""
Выполни АКТИВНОЕ функциональное UI тестирование (не просто скриншоты!).

**⚠️ ВАЖНО: Ты должен АКТИВНО тестировать, а не просто смотреть!**

## 1. Базовая проверка
- Открой каждую затронутую страницу через Playwright MCP
- Проверь консоль браузера на ошибки
- Сделай скриншоты

## 2. Тестирование форм (ОБЯЗАТЕЛЬНО)

Для КАЖДОЙ формы на странице:

**Happy path:**
- Заполни корректными данными → отправь → проверь результат

**Попробуй сломать валидацию:**
- Пустые обязательные поля
- Неправильный email: `test`, `@test`, `test@`, `test@@test.com`
- Отрицательные числа, ноль, очень большие числа
- Даты в неправильном диапазоне

**Edge cases:**
- Очень длинные строки (1000+ символов) — ломает ли вёрстку?
- Спецсимволы: `<script>alert(1)</script>`
- SQL injection: `'; DROP TABLE users; --`
- Unicode: `тест`, `测试`, emoji `🎉`
- Только пробелы, пробелы в начале/конце

## 3. Тестирование кнопок и действий

Для КАЖДОЙ кнопки:
- Нажми и проверь результат
- Double-click — не дублируется ли действие?
- Кнопка удаления — есть ли подтверждение?

## 4. Тестирование навигации

- Все ссылки работают
- Кнопка "Назад" браузера работает
- Breadcrumbs корректны (если есть)

## 5. Тестирование состояний

**Пустое состояние:**
- Как выглядит без данных?
- Есть ли понятное сообщение?

**Много данных:**
- Пагинация работает?
- Таблицы не разъезжаются?

## 6. Полный user flow

Пройди весь путь пользователя:
1. Зайти на страницу
2. Создать запись через форму
3. Проверить что создалась
4. Отредактировать
5. Удалить
6. Проверить что удалилась

## Формат отчёта

### Forms Tested
| Form | Happy Path | Validation | Edge Cases | Result |
|------|------------|------------|------------|--------|
| ... | ... | ... | ... | ... |

### Issues Found
1. **CRITICAL/HIGH/MEDIUM/LOW** - описание проблемы

### User Flows Tested
- [x] Flow 1: описание
- [ ] Flow 2: описание (если не прошёл)
""",
  model="opus"
)
```

### Шаг 2: Исправь найденные проблемы

Для каждой проблемы из UI Review:
- ✅ **CRITICAL/HIGH** — исправить обязательно
- ✅ **MEDIUM** — исправить если возможно
- 📋 **LOW / Вне scope** — создать новую задачу

### Шаг 3: Повторная проверка

После исправлений:
1. Повтори тесты для исправленных элементов
2. Убедись что исправления не сломали другое
3. Сделай контрольные скриншоты

## Phase 6: Reviews (ОБЯЗАТЕЛЬНО через ralph review)

### 🚨 КРИТИЧЕСКИ ВАЖНО

**ВСЕ review ОБЯЗАТЕЛЬНО запускаются через ralph CLI.**

Ты **НЕ МОЖЕШЬ**:
- ❌ Делать code review самостоятельно
- ❌ Решать что "reviews не применимы"
- ❌ Пропускать этот этап для "простых" изменений
- ❌ Заменять ralph review на Task с агентами

### Запуск (ЕДИНСТВЕННЫЙ правильный способ)

```bash
Bash(command="ralph review {project}#{number}")
```

Команда запускает 3 сессии (codex review выполняется отдельно на уровне CLI):
1. `/ralph-review-code` — 5 агентов параллельно
2. `/ralph-review-simplify` — code-simplifier
3. `/ralph-review-security` — security review

> **Примечание:** Codex review выполняется автоматически на уровне `ralph implement` CLI после завершения всего workflow.

### Проверка результатов

После завершения **ОБЯЗАТЕЛЬНО**:

```python
task = get_task(project, number)
review = task.get("review", "")

# Review должен содержать результаты от всех 4 команд
if not review or "Code Review" not in review:
    # БЛОКИРОВКА - reviews не выполнены
```

### Если ralph review не работает

**НЕ делай свой review!** Вместо этого:
1. Запиши ошибку в blocks
2. Установи status=hold
3. ВЫЙТИ

### Обработка замечаний (ОБЯЗАТЕЛЬНО для КАЖДОГО пункта)

**⚠️ КАЖДОЕ замечание** из review (любого уровня: CRITICAL, HIGH, MEDIUM, LOW, INFO) **ДОЛЖНО** иметь явную отметку в поле `review` задачи:

```python
# После обработки замечаний обнови review поле
update_task(
    project="<project>",
    number=<N>,
    review=existing_review + "\n\n### [main-claude] Response\n" + response_text
)
```

**Формат отметки для каждого замечания:**

⚠️ **Смайлы ОБЯЗАТЕЛЬНЫ** — они позволяют быстро выделить результат в стене текста!

```markdown
### [main-claude] Response

#### Code Simplifier
1. **Дублирование get_test_db_connection()** (High)
   → ✅ [main-claude] Fixed: удалён, используется fixture из conftest.py

2. **Константы дублируются** (Medium)
   → 📋 [main-claude] Deferred to #42: вынос констант в общий модуль

3. **Упрощение f-string в SQL** (Medium)
   → ❌ [main-claude] Declined: константы модуля, не пользовательский ввод, безопасно

4. **Избыточные комментарии-разделители** (Low)
   → ❌ [main-claude] Declined: улучшают навигацию в длинном скрипте

#### Security Review
1. **SQL Injection через String Interpolation** (Low)
   → ❌ [main-claude] Declined: значения — модульные константы, скрипт только для test DB
```

**Варианты действий (всегда со смайлом в начале!):**
- ✅ `✅ [main-claude] Fixed: <что исправлено>` — внесены изменения в код
- 📋 `📋 [main-claude] Deferred to #N: <причина>` — создана отдельная задача
- ❌ `❌ [main-claude] Declined: <обоснование>` — замечание не применимо/некорректно

**⚠️ ЗАПРЕЩЕНО:**
- Игнорировать замечания без отметки
- Писать общие фразы типа "все рекомендации учтены"
- Обрабатывать только CRITICAL/HIGH, пропуская остальные

## Phase 7: Final Testing

**После всех исправлений** запусти тесты, связанные с текущей задачей.

**⚠️ ВАЖНО**: Полный набор тестов запускается в `/ralph-batch-check` после пула задач. Здесь проверяем только релевантные тесты.

### Что тестировать

1. **Новые тесты** — написанные в этой задаче
2. **Связанные тесты** — затрагивающие изменённые модули
3. **Регрессия** — тесты компонентов, которые могли быть затронуты

### Команда запуска
```bash
# Тесты конкретных модулей
pytest path/to/tests/ -v

# Или по ключевому слову
pytest -k "test_feature_name" -v
```

### UI проверка (если есть frontend)

Финальная визуальная проверка через Playwright MCP:
1. Открой затронутые страницы
2. Сделай контрольные скриншоты
3. Убедись что UI проблемы из Phase 5 исправлены

**Все запущенные тесты должны проходить** перед переходом к линтерам.

## Phase 8: Linters

**После успешных тестов** запусти линтеры через команду /python-linters.

**⚠️ ВАЖНО**: Передай путь к проекту в промпте:

```
Task(
  subagent_type="general-purpose",
  prompt="""
Выполни /python-linters для проекта.

Путь к проекту: {project_path}

Верни список замечаний.
""",
  model="opus"
)
```

**Исправь все ошибки линтеров** перед переходом к документации.

Игнорируемые правила djlint (см. pyproject.toml):
- H006: img без width/height (Bootstrap responsive)
- H019: javascript: в href (pagination links)
- H021: Inline styles (для динамических стилей)
- H030/H031: meta/script в head (Jinja2)
- J018: Jinja2 whitespace
- T003: endblock без имени

## Phase 9: Cleanup

**После линтеров** выполни очистку перед сдачей:

### Шаг 1: Проверь и удали ошибочно созданные файлы

Проверь `git status` на наличие мусорных файлов, созданных по ошибке:

```bash
git status --porcelain | grep "^??"
```

**Типичные признаки мусорных файлов:**
- Файлы с пробелами/спецсимволами в имени: `"Exit code: "`, `"some output"`
- Файлы с именами bash-команд: `echo`, `cat`, `grep`, `test`, `true`, `false`
- Файлы с цифрами или случайными именами: `1`, `123`, `output`
- Пустые файлы без расширения в неожиданных местах

**Примеры мусора (удалить):**
```bash
rm -f "Exit code: " echo cat grep test true false
rm -f 1 2 3 output result tmp temp
```

**НЕ являются мусором** (легитимные файлы без расширения):
- `README`, `LICENSE`, `Makefile`, `Dockerfile`, `Procfile`
- `.env.example`, `.gitignore`, `.dockerignore`
- Файлы в `.git/`, `node_modules/`, `venv/`

**Если нашёл мусорные файлы — удали их перед коммитом.**

### Шаг 2: Проверь и исправь разрешения изменённых файлов

Используй `git status` для получения списка изменённых файлов:

```bash
# Получи список изменённых/новых файлов
git status --porcelain | awk '{print $2}'

# Проверь разрешения для каждого типа файлов
# Python модули должны быть 644 (rw-rw-r--)
# Shell скрипты должны быть 755 (rwxrwxr-x)
```

Для исправления разрешений:
```bash
chmod 644 path/to/file.py
chmod 755 path/to/script.sh
```

**Стандартные разрешения:**
| Тип файла | Права | Описание |
|-----------|-------|----------|
| `*.py` | 644 | Python модули |
| `*.sh` | 755 | Shell скрипты (исполняемые) |
| `*.html`, `*.css`, `*.js` | 644 | Web assets |
| `*.sql`, `*.yml`, `*.json` | 644 | Конфигурация и данные |
| `*.md` | 644 | Документация |

### Шаг 3: Отметь cleanup как завершённый

**ОБЯЗАТЕЛЬНО** напиши в отчете в фазе 11 фразу **"cleanup completed"** — это маркер для workflow hook.

## Phase 10: Documentation

Обнови документацию если нужно:
- Architecture docs
- README
- CLAUDE.md
- User help/справка

### CHANGELOG.md (ОБЯЗАТЕЛЬНО)

**Добавь запись в CHANGELOG.md** о выполненных изменениях:

```markdown
## [Unreleased]

### Добавлено
- Новая функциональность

### Изменено
- Изменения в существующей функциональности

### Исправлено
- Исправленные баги
```

**Формат записи:**
- Краткое описание изменения (1 строка)
- Ссылка на задачу если есть: `(#N)`
- Группируй по категориям: Добавлено, Изменено, Исправлено, Удалено, Безопасность

**Пример:**
```markdown
### Добавлено
- Фильтры по локациям и подразделениям на странице событий (#6)
- Экспорт посещаемости в Excel с учётом фильтров (#6)

### Исправлено
- Автопереподключение камеры при потере связи (#7)
```

### CLAUDE.md (ОБЯЗАТЕЛЬНО при решении проблем)

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Если во время работы ты столкнулся с проблемами — **ОБЯЗАТЕЛЬНО** запиши решения!

#### Checklist — что ОБЯЗАТЕЛЬНО записать:

| Ситуация | Действие |
|----------|----------|
| Потратил >2 попытки на команду/синтаксис | → Записать правильный синтаксис |
| Нашёл правильный хост/порт методом проб | → Записать рабочий адрес |
| Обнаружил особенность API/библиотеки | → Записать с примером |
| Workaround для ошибки инфраструктуры | → Записать причину и решение |
| Неочевидный порядок действий | → Записать последовательность |

#### Формат записи в CLAUDE.md:

```markdown
## Известные проблемы и решения

### Запуск UI тестов для конкретного класса
**Проблема:** `run_tests.sh ui/test_file::TestClass` не работает
**Решение:** Использовать `-k`: `run_tests.sh ui/test_file.py -k TestClass`

### Подключение Playwright к тестовому серверу
**Проблема:** 127.0.0.1 и host.docker.internal не работают
**Решение:** Использовать `web-test:8080` (имя сервиса в docker-compose)
```

#### Куда записывать:

- **Общие проблемы проекта** → `/workspace/CLAUDE.md`
- **Проблемы конкретного сервиса** → `/workspace/services/<service>/CLAUDE.md`
- **Проблемы тестов** → `/workspace/services/<service>/tests/CLAUDE.md`

**⚠️ НЕ ПРОПУСКАЙ этот шаг!** Следующий агент потратит время на те же проблемы, если ты не запишешь решение.

## Phase 11: Complete Task

### Шаг 1: Создай коммит

**Используй команду `/commit`** для создания коммита в стиле репозитория:

```
Skill(skill="commit")
```

Команда автоматически:
- Проанализирует стиль коммитов в репозитории
- Добавит изменённые файлы (без .env и credentials)
- Создаст коммит с правильным форматом сообщения
- Добавит Co-Authored-By если используется в репо

### Шаг 2: CONFIRMATION PHRASE (ОБЯЗАТЕЛЬНО ПЕРЕД update_task!)

**⚠️ КРИТИЧЕСКИ ВАЖНО: Напиши confirmation phrase ДО update_task!**

Это защита от крашей: если сессия упадёт после update_task но до confirmation,
hook увидит status=done но без подтверждения и заблокирует.

```
I confirm that all task phases are fully completed.
```

**Порядок важен:**
1. ✅ Сначала confirmation phrase (подтверждает что ВСЁ готово)
2. ✅ Потом update_task с status=done

### Шаг 3: Запиши отчёт в задачу

Запиши отчёт в поле `report` (не в body!):

```python
# 1. Получи информацию о коммите
# git log -1 --format='%H %s' → commit_hash commit_message

# 2. Подготовь отчёт
report_text = """
### Commit
- Hash: {commit_hash}
- Message: {commit_message}

### Выполненная работа
- [Что сделано]

### Изменённые файлы
- file1.py — описание
- file2.py — описание

### Тестирование
- Автотесты: [результат]
- Ручное тестирование: [что проверено]
"""

# 3. Обнови задачу и переведи в done
update_task(
    project="<project>",
    number=<N>,
    status="done",
    completed="<YYYY-MM-DD>",
    report=report_text  # ← поле report, НЕ body!
)
```

### Шаг 4: Выведи финальный отчёт

```
✅ ЗАДАЧА ЗАВЕРШЕНА

📋 Задача: {project}#{number} - {title}
📊 Статус: done

📝 Коммит: {commit_hash[:8]} - {commit_message}

📁 Изменённые файлы:
- path/to/file1.py
- path/to/file2.py

cleanup completed
```

**Пользователь сам переведёт задачу в "human approved" после проверки.**

## Checklist

### Preparation (Phase 0-2)
- [ ] Задача получена и содержит план в поле `plan`
- [ ] **CLAUDE.md прочитаны** для затрагиваемых сервисов
- [ ] Статус задачи = work
- [ ] TodoWrite создан для отслеживания фаз (0-11)
- [ ] Файлы из Scope прочитаны

### Implementation (Phase 3)
- [ ] Implementation выполнен по плану

### Testing (Phase 4)
- [ ] Unit tests написаны и проходят
- [ ] API tests написаны (если есть endpoints)
- [ ] UI tests написаны с data-testid (если есть frontend)
- [ ] Edge cases покрыты тестами
- [ ] Existing tests не сломаны

### Reviews (Phase 6)
- [ ] `ralph review` выполнен (3 сессии; codex review — автоматически на уровне CLI)
- [ ] Все замечания обработаны с отметками `[main-claude]`
- [ ] Каждое замечание имеет: Fixed / Deferred to #N / Declined

### Testing (Phase 7)
- [ ] Связанные тесты проходят после исправлений

### Documentation (Phase 10)
- [ ] CHANGELOG.md обновлён
- [ ] **Если были проблемы** → решения записаны в CLAUDE.md
- [ ] **Если >2 попыток на команду** → правильный синтаксис записан

### Finalization (Phase 9, 11)
- [ ] Linters проходят (ruff, djlint)
- [ ] Cleanup выполнен (мусор удалён, разрешения проверены)
- [ ] Коммит создан
- [ ] Report с commit hash записан в поле `report` (status=done)
- [ ] Финальный отчёт выведен

## ⚠️ ОБЯЗАТЕЛЬНОЕ ПОДТВЕРЖДЕНИЕ

**ПЕРЕД update_task со status=done** напиши фразу:

```
I confirm that all task phases are fully completed.
```

**⚠️ ПОРЯДОК КРИТИЧЕН:**
1. Убедись что ВСЕ фазы выполнены (commit создан, всё готово)
2. Напиши confirmation phrase
3. Только ПОТОМ вызывай update_task с status=done

Эта фраза сигнализирует hook'у что workflow завершён. Без неё workflow будет заблокирован.

## Error Handling

### Критические ошибки → blocks + hold

Следующие ситуации требуют **записи в blocks и статуса hold**:

| Ситуация | Действие |
|----------|----------|
| Codex недоступен | blocks + hold → ВЫЙТИ |
| Code review не выполнен | blocks + hold → ВЫЙТИ |
| Security review не выполнен | blocks + hold → ВЫЙТИ |
| md-task-mcp недоступен | blocks + hold → ВЫЙТИ |
| Непонятные требования в плане | blocks + hold → ВЫЙТИ |
| Неразрешимый конфликт с кодом | blocks + hold → ВЫЙТИ |

**ЗАПРЕЩЕНО:**
- Заменять недоступный инструмент собственным анализом
- Записывать своё ревью вместо codex/code-review
- Продолжать workflow при критической ошибке
- Игнорировать ошибки "инструмент недоступен"
- Использовать AskUserQuestion
- Ждать ответа пользователя

### Некритические ошибки (продолжать автономно)

| Ситуация | Действие |
|----------|----------|
| Замечание от ревью спорное | Исправить по best practices |
| Тесты падают | Исправлять итеративно пока не пройдут |
| Документация неполная | Обновить и продолжить |
| Linter ошибки | Исправить и продолжить |

### Паттерн blocks + hold

```bash
# 1. Сохранить текущую работу
git add -A
git commit -m "WIP: {project}#{number} - blocked: {краткое описание}"
```

```python
# 2. Записать блокировку (используй поле blocks!)
blocks_text = """
- [{YYYY-MM-DD HH:MM}] {Краткое описание проблемы}
  - Что пытался сделать: {описание действий}
  - Почему не получилось: {причина блокировки}
  - Что нужно от человека: {конкретный запрос}
  - Коммит с незавершённой работой: {commit_hash}
"""

update_task(
    project="<project>",
    number=<N>,
    status="hold",
    blocks=blocks_text  # ← поле blocks, НЕ body!
)
```

**После записи блокировки — ВЫЙТИ. Не пытаться продолжать.**
