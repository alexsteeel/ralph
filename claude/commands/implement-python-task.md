---
name: execute-python-task
description: Execute Python task from ralph-tasks
arguments:
  - name: task_ref
    description: Task reference in format "project#N" (e.g., "myproject#18")
    required: true
---

Ты выполняешь Python-задачу с полным workflow: планирование, одобрение, реализация, тестирование, документация.

## 🚨 PRODUCTION QUALITY — НЕ MVP!

**Это PRODUCTION код, а не MVP/прототип.**

### Обязательные требования

1. **ВСЕ 13 ФАЗ должны быть выполнены** — пропуск этапов ЗАПРЕЩЁН
2. **ВСЕ тесты должны проходить** — не "большинство", а ВСЕ
3. **ВСЕ замечания от ревью должны быть обработаны** — каждое с отметкой `[main-claude]: Fixed/Deferred/Declined`
4. **ВСЕ ошибки линтеров должны быть исправлены** — код должен быть чистым
5. **Качество кода** — следуй паттернам проекта, не срезай углы

### ЗАПРЕЩЕНО

- ❌ Пропускать фазы "для скорости"
- ❌ Оставлять failing tests "на потом"
- ❌ **Помечать тесты как `@pytest.mark.skip` чтобы обойти падающие тесты**
- ❌ Игнорировать замечания ревью
- ❌ Писать TODO/FIXME вместо реализации
- ❌ Упрощать реализацию относительно плана без согласования

### 🚨 КРИТИЧНО: Skipped tests = FAILED TASK

**`@pytest.mark.skip` разрешён ТОЛЬКО если:**
- Тест был skip ДО начала текущей задачи
- Тест требует внешней инфраструктуры (CI, staging)

**`@pytest.mark.skip` ЗАПРЕЩЁН для:**
- Новых тестов, которые не проходят
- "Сложных" тестов, которые не хочется отлаживать

### Если не можешь выполнить качественно

**НЕ ДЕЛАЙ ПЛОХО** — лучше сообщи пользователю о проблеме с "need feedback", чем сдать некачественную работу.

## ⚠️ ТРЕБОВАНИЯ К МОДЕЛИ

**Для планирования и реализации используй Opus 4.5** — это обеспечивает максимальное качество анализа и кода.

**Для планирования (Phase 1) ОБЯЗАТЕЛЬНО используй ultrathink mode** — extended thinking для глубокого анализа архитектуры и создания детального плана.

## Используемые инструменты

| Инструмент | Фаза | Контекст |
|------------|------|----------|
| `ralph-tasks` | 0, 2, 12 | основной |
| `TodoWrite` | 0-12 | основной (отслеживание прогресса) |
| `EnterPlanMode`, `ExitPlanMode` | 1 | **plan mode** |
| `context7`, `pyright-lsp`, `playwright` | 3 | основной |
| `/pr-review-toolkit:review-pr` | 5 | основной |
| `/security-review` | 6 | основной |
| `/codex-review` | 7 | **изолированный** |
| `Bash` (chmod) | 10 | основной (cleanup) |

## ⚠️ ВХОДНЫЕ ДАННЫЕ

**Task reference:** `$ARGUMENTS`

Задача ДОЛЖНА существовать в ralph-tasks. Формат: `project#N`.

## Workflow

0. Get Task
1. Plan Mode (Analyze + Plan + Approval)
2. Update Task
3. Implementation
4. Initial Testing
5. Code Review (/pr-review-toolkit:review-pr)
6. Security Review (/security-review)
7. Codex Review
8. Final Testing
9. Linters
10. Cleanup
11. Documentation
12. Complete

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Непрерывное выполнение

**НЕ ОСТАНАВЛИВАЙСЯ** между этапами workflow. Паузы допустимы ТОЛЬКО в определённых точках.

### Как запросить обратную связь от пользователя

Если тебе **действительно нужен ответ пользователя** (уточнение, выбор варианта, одобрение):
- Включи фразу **"need feedback"** в своё сообщение
- Это позволит остановиться и дождаться ответа пользователя

Примеры когда использовать "need feedback":
- Есть несколько вариантов реализации и нужен выбор пользователя
- Возникла критическая ошибка и нужно решение

**НЕ ИСПОЛЬЗУЙ "need feedback"** просто для отчёта о прогрессе — продолжай выполнение.

### Точки остановки

| Phase | Название | Действие |
|-------|----------|----------|
| 0 | Get Task | ✅ Авто-продолжение |
| 1 | Plan Mode | ⏸️ **СТОП — жди ответа пользователя** |
| 2 | Update Task | ✅ Авто-продолжение |
| 3 | Implementation | ✅ Авто-продолжение |
| 4 | Initial Testing | ✅ Авто-продолжение |
| 5 | Code Review | ✅ Авто-продолжение |
| 6 | Security Review | ✅ Авто-продолжение |
| 7 | Codex Review | ✅ Авто-продолжение |
| 8 | Final Testing | ✅ Авто-продолжение |
| 9 | Linters | ✅ Авто-продолжение |
| 10 | Cleanup | ✅ Авто-продолжение |
| 11 | Documentation | ✅ Авто-продолжение |
| 12 | Complete Task | ⏸️ **СТОП — жди подтверждения пользователя** |

**После завершения каждого этапа (включая skill/agent вызовы) — НЕМЕДЛЕННО переходи к следующему этапу без ожидания ответа пользователя.**

Исключения:
- Phase 1 (Plan Mode): ExitPlanMode → ЖДИ "да/нет/изменить"
- Phase 12: показал отчёт → ЖДИ "да/доработать"

## Phase 0: Get Task

Получи задачу через ralph-tasks:

```python
task = get_task(project="<project>", number=<N>)
```

**ЗАТЕМ** создай todo list для отслеживания прогресса через `TodoWrite`:

```
TodoWrite([
  { content: "Phase 0: Get Task", status: "completed", activeForm: "Getting task" },
  { content: "Phase 1: Plan Mode", status: "in_progress", activeForm: "Planning implementation" },
  { content: "Phase 2: Update Task", status: "pending", activeForm: "Updating task status" },
  { content: "Phase 3: Implementation", status: "pending", activeForm: "Implementing feature" },
  { content: "Phase 4: Initial Testing", status: "pending", activeForm: "Running initial tests" },
  { content: "Phase 5: Code Review", status: "pending", activeForm: "Running code review" },
  { content: "Phase 6: Security Review", status: "pending", activeForm: "Running security review" },
  { content: "Phase 7: Codex Review", status: "pending", activeForm: "Running Codex review" },
  { content: "Phase 8: Final Testing", status: "pending", activeForm: "Running final tests" },
  { content: "Phase 9: Linters", status: "pending", activeForm: "Running linters" },
  { content: "Phase 10: Cleanup", status: "pending", activeForm: "Cleaning up" },
  { content: "Phase 11: Documentation", status: "pending", activeForm: "Updating documentation" },
  { content: "Phase 12: Complete Task", status: "pending", activeForm: "Completing task" }
])
```

**ОБНОВЛЯЙ TODO LIST** при переходе между фазами:
- Текущая фаза → `in_progress`
- Завершённая фаза → `completed`

## Phase 1: Plan Mode (Analysis + Planning + Approval)

**ИСПОЛЬЗУЙ ВСТРОЕННЫЙ PLAN MODE** для анализа и планирования.

**⚠️ ULTRATHINK MODE ОБЯЗАТЕЛЕН**: При входе в Plan Mode активируй extended thinking (ultrathink) для глубокого анализа архитектуры, поиска оптимальных решений и создания детального плана реализации.

### Шаг 0: Анализ зависимостей между задачами

**Перед планированием** проанализируй связи с другими задачами:

```python
# Получи все задачи проекта
tasks(project="<project>")
```

**Проверь:**

1. **Явные связи (depends_on)**:
   - Задачи, от которых зависит текущая (блокираторы) — проверь их статус
   - Задачи, которые зависят от текущей (будут разблокированы)

2. **Неявные связи** (семантический анализ):
   - По модулю: задачи с тем же `module`
   - По целям: задачи с похожим функционалом
   - Возможности переиспользования: общие паттерны, утилиты

**Если найдены значимые связи** — обсуди с пользователем через AskUserQuestion:
- Нужно ли добавить depends_on?
- Стоит ли планировать общие паттерны?

### Шаг 1: Войди в Plan Mode

```
Вызови EnterPlanMode tool
```

### Шаг 2: В Plan Mode выполни анализ

В режиме планирования исследуй кодовую базу:

1. **Прочитай проектную документацию**:
   - CLAUDE.md — правила разработки
   - README.md — структура проекта
   - docs/*.md — архитектура

2. **Проанализируй существующий код**:
   - Используй Glob, Grep, Read для поиска паттернов
   - Найди связанные компоненты и зависимости
   - Определи точки интеграции

3. **Создай план реализации**:
   - **Scope**: какие файлы/компоненты затрагиваем
   - **Implementation Steps**: пошаговый план
   - **Testing Strategy**: что и как тестируем
   - **Documentation Updates**: что обновить в документации

   **⚠️ НЕ УКАЗЫВАЙ НОМЕРА СТРОК В ПЛАНЕ!**

   Номера строк становятся неактуальными после изменений. Используй устойчивые маркеры:
   - Имена функций/классов/методов: `в функции get_user_by_id()`
   - Паттерны кода: `после блока валидации`, `в секции импортов`
   - Структурные элементы: `в конце класса UserService`, `перед return`
   - Относительные позиции: `рядом с existing_function()`, `по аналогии с create_user()`

4. **Уточни все неясные моменты**:
   - Используй AskUserQuestion для прояснения **всех** спорных, непонятных или неоднозначных моментов
   - Если есть несколько вариантов реализации — спроси какой предпочтительнее
   - Уточни детали если требования неполные или противоречивые
   - Обсуди сомнения в архитектуре с пользователем
   - **НЕ НАЧИНАЙ реализацию пока есть неясности** — лучше спросить сейчас, чем переделывать потом

5. **Оцени размер задачи**:
   - Если задача требует изменений в **более чем 5-7 файлах** или **затрагивает несколько модулей/подсистем** — предложи разбиение на подзадачи
   - Каждая подзадача должна быть **автономной** и **тестируемой отдельно**
   - Используй AskUserQuestion чтобы согласовать разбиение с пользователем
   - При согласии — создай подзадачи через `create_task()` и свяжи их через `depends_on`

### Шаг 3: Выйди из Plan Mode

```
Вызови ExitPlanMode tool
```

**После ExitPlanMode пользователь увидит план и должен его одобрить.**

**НЕ НАЧИНАЙ РЕАЛИЗАЦИЮ БЕЗ ЯВНОГО ОДОБРЕНИЯ ПОЛЬЗОВАТЕЛЯ!**

## Phase 2: Update Task

После одобрения плана обнови задачу через `mcp__ralph-tasks__update_task`:

```python
# План записывается в поле plan, НЕ в body!
update_task(
    project="<project>",
    number=<N>,
    status="work",                    # Переводим в работу
    started="<YYYY-MM-DD>",          # Сегодняшняя дата
    plan=plan_text                    # ← ВАЖНО: поле plan, НЕ body!
)
```

**⚠️ Структурированные поля задачи:**
- `plan` — план реализации (Phase 2)
- `report` — отчёт о выполнении (Phase 12)
- `review` — результаты code review (Phase 5-7)

**НЕ записывай план и отчёт в body!**

## Phase 3: Implementation

### Шаг 1: ОБЯЗАТЕЛЬНО прочитай CLAUDE.md (ПЕРЕД любой работой!)

**⚠️ СТОП! Сначала определи затрагиваемые сервисы из Scope в плане.**

**ОБЯЗАТЕЛЬНО вызови Read tool для КАЖДОГО файла:**

1. `Read("/workspace/CLAUDE.md")` — **ВСЕГДА**
2. `Read("/workspace/services/<service>/CLAUDE.md")` — для каждого сервиса из Scope
3. `Read("/workspace/services/<service>/tests/CLAUDE.md")` — если будут тесты

**⚠️ ЭТО НЕ ОПЦИОНАЛЬНО!** Там находятся:
- Точный синтаксис запуска тестов
- Правильные хосты/порты для Playwright
- Решения известных проблем

**🚫 ЗАПРЕЩЕНО:**
- Пропускать CLAUDE.md сервиса
- Угадывать синтаксис команд вместо чтения документации

### Шаг 2: Реализация

Реализуй по плану. Следуй паттернам и стилю текущего репозитория.

Доступные инструменты:
- **context7** — документация библиотек
- **LSP** — статический анализ Python (типы, определения, ссылки)
- **Playwright MCP** — тестирование UI

### Обнаружение проблем в коде

При обнаружении проблем **не относящихся к текущей задаче** (баги, технический долг, уязвимости):

1. **НЕ исправляй** проблему в рамках текущей задачи
2. **Создай новую задачу** с описанием проблемы:

```python
create_task(
    project="<project>",
    description="<Краткое описание проблемы>",
    body="""
### Проблема
<Что обнаружено>

### Где найдено
- Файл: `path/to/file.py`

### Контекст обнаружения
Найдено при выполнении задачи #{current_task_number}
"""
)
```

3. **Продолжай текущую задачу** — новая задача будет обработана отдельно

## Phase 4: Initial Testing

**После реализации** создай и запусти тесты. Следуй паттернам существующих тестов.

### Обязательные типы тестов

#### 1. Unit Tests
- Тесты отдельных функций и методов
- Моки для внешних зависимостей
- Покрытие основных сценариев (happy path)
- Покрытие edge cases (граничные случаи, пустые данные, None, ошибки)

#### 2. API Tests (если есть endpoints)
- Тесты HTTP endpoints (GET, POST, PUT, DELETE)
- Проверка response codes (200, 400, 401, 403, 404, 500)
- Валидация request/response schema
- Тесты с авторизацией и без

#### 3. UI Tests (ОБЯЗАТЕЛЬНО если есть frontend)

**⚠️ ОБЯЗАТЕЛЬНО используй `data-testid` для всех тестируемых элементов!**

```html
<!-- В HTML/шаблонах добавляй data-testid -->
<button data-testid="submit-button">Submit</button>
<input data-testid="email-input" type="email" />
<div data-testid="user-profile">...</div>
```

```python
# В тестах используй data-testid для надёжных селекторов
page.click('[data-testid="submit-button"]')
page.fill('[data-testid="email-input"]', 'test@example.com')
expect(page.locator('[data-testid="user-profile"]')).to_be_visible()
```

**Преимущества data-testid:**
- Устойчивость к изменениям CSS классов и структуры
- Явное обозначение тестируемых элементов
- Не зависит от текста (локализация)

**Playwright тесты покрывают:**
- Критические user flows
- Формы и валидацию
- Навигацию между страницами

#### Активное UI тестирование (не просто скриншоты!)

**ЗАПРЕЩЕНО:**
- ❌ Просто открыть страницу и сделать скриншот
- ❌ Написать "UI выглядит корректно" без активного тестирования

**ОБЯЗАТЕЛЬНО:**
- ✅ Прокликать ВЕСЬ функционал
- ✅ Заполнить ВСЕ формы разными данными
- ✅ Попытаться СЛОМАТЬ валидацию
- ✅ Проверить edge cases

**Для КАЖДОЙ формы:**

1. **Happy path**: корректные данные → отправка → проверка результата
2. **Валидация**: пустые поля, неправильный email, отрицательные числа
3. **Edge cases**: очень длинные строки, спецсимволы, unicode

**Для КАЖДОЙ кнопки:**
- Нажать и проверить результат
- Double-click — не дублируется ли действие?
- Кнопка удаления — есть ли подтверждение?

### Чеклист тестов

- [ ] **Happy path**: основной сценарий работает
- [ ] **Edge cases**: пустые данные, граничные значения
- [ ] **Error handling**: ошибки обрабатываются корректно
- [ ] **Permissions**: права доступа проверены
- [ ] **Existing tests**: старые тесты не сломаны

**Все тесты должны проходить** перед переходом к ревью.

## Phase 5: Code Review

Запусти `/pr-review-toolkit:review-pr` для комплексного код-ревью.

Плагин включает проверки:
- Баги и логические ошибки
- Silent failures и error handling
- Качество новых типов
- Корректность комментариев
- Полнота тестов

### Code Simplifier

**После основного ревью** запусти агент упрощения кода:

```
Task(
  subagent_type="pr-review-toolkit:code-simplifier",
  prompt="Проанализируй изменения и найди возможности упрощения кода.",
  model="opus"
)
```

Агент проверяет:
- Избыточную сложность
- Дублирование кода
- Возможности использования стандартных паттернов
- Лишние абстракции

### Обработка результатов (ОБЯЗАТЕЛЬНО для КАЖДОГО пункта)

**⚠️ КАЖДОЕ замечание** из review (любого уровня: CRITICAL, HIGH, MEDIUM, LOW, INFO) **ДОЛЖНО** иметь явную отметку:

```python
# Запиши результаты в поле review задачи
update_task(
    project="<project>",
    number=<N>,
    review=existing_review + "\n\n### [main-claude] Response\n" + response_text
)
```

**Формат отметки для каждого замечания:**

```markdown
### [main-claude] Response

#### Code Review
1. **Дублирование get_test_db_connection()** (High)
   → [main-claude] Fixed: удалён, используется fixture из conftest.py

2. **Константы дублируются** (Medium)
   → [main-claude] Deferred to #42: вынос констант в общий модуль

3. **Избыточные комментарии** (Low)
   → [main-claude] Declined: улучшают навигацию в длинном скрипте
```

**Варианты действий:**
- ✅ `[main-claude] Fixed: <что исправлено>` — внесены изменения в код
- 📋 `[main-claude] Deferred to #N: <причина>` — создана отдельная задача
- ❌ `[main-claude] Declined: <обоснование>` — замечание не применимо/некорректно

**⚠️ ЗАПРЕЩЕНО:**
- Игнорировать замечания без отметки
- Писать общие фразы типа "все рекомендации учтены"
- Обрабатывать только CRITICAL/HIGH, пропуская остальные

**Все замечания должны быть обработаны** перед продолжением.

## Phase 6: Security Review

Запусти встроенную команду `/security-review` для security-аудита изменений.

Security review проверяет:
- Уязвимости OWASP Top 10
- SQL injection, XSS, command injection
- Утечки секретов и credentials
- Небезопасные зависимости
- Проблемы с аутентификацией/авторизацией

### Обработка результатов

1. **Запиши результаты в задачу** через ralph-tasks:
   ```markdown
   ## Security Review
   - [Найденные уязвимости]
   ```

2. **Для каждого замечания**:
   - ✅ **Исправить** — устранить уязвимость
   - ❌ **Отклонить** — записать причину почему замечание некорректно (false positive)

**Все замечания должны быть обработаны** перед продолжением.

## Phase 7: Codex Review (изолированный контекст)

Запусти через Task tool (Opus 4.5).

**⚠️ ВАЖНО**: Подставь реальные значения проекта и номера задачи:

```
Task(
  subagent_type="general-purpose",
  prompt="""
Выполни /codex-review {project_name}#{task_number}.

Верни список замечаний.
""",
  model="opus"
)
```

Например: `/codex-review myproject#18`

### ⚠️ КРИТИЧЕСКАЯ ПРОВЕРКА

После выполнения агента **ОБЯЗАТЕЛЬНО ПРОВЕРЬ**:

1. **Codex был успешно запущен?**
   - Если codex недоступен, упал, или вернул ошибку → **КРИТИЧЕСКАЯ ОШИБКА**
   - **НЕ ПРОДОЛЖАЙ** к следующим фазам
   - **НЕ ЗАМЕНЯЙ** codex-ревью своим собственным анализом
   - Сообщи пользователю с фразой need feedback для stop hook и жди решения

2. **Review записан в задачу?**
   - Проверь что в задаче появилась секция Review от Codex
   - Если секция отсутствует → агент не выполнил задачу корректно

### Обработка результатов

**Для каждого замечания от Codex**:
- ✅ **Исправить** — внести изменения в код
- ❌ **Отклонить** — записать причину в задачу почему замечание некорректно

**Все замечания должны быть обработаны** перед продолжением.

## Phase 8: Final Testing

**После всех исправлений** повторно запусти полный набор тестов:

### Финальная проверка

1. **Unit tests** — все тесты проходят
2. **API tests** — endpoints работают корректно
3. **UI tests** — Playwright тесты проходят
4. **Regression** — существующие тесты не сломаны

### Команда запуска
```bash
# Запусти все тесты проекта
pytest  # или команда из проекта
```

**Все тесты должны проходить** перед переходом к линтерам.

## Phase 9: Linters

**После успешных тестов** запусти линтеры через команду /python-linters.

**⚠️ ВАЖНО**: Передай путь к проекту в промпте:

```
Task(
  subagent_type="general-purpose",
  prompt="""
Выполни /python-linters для проекта.

Путь к проекту: {project_path}

Верни список замечаний.
""",
  model="opus"
)
```

**Исправь все ошибки линтеров** перед переходом к документации.

Игнорируемые правила djlint (см. pyproject.toml):
- H006: img без width/height (Bootstrap responsive)
- H019: javascript: в href (pagination links)
- H021: Inline styles (для динамических стилей)
- H030/H031: meta/script в head (Jinja2)
- J018: Jinja2 whitespace
- T003: endblock без имени

## Phase 10: Cleanup

**После линтеров** выполни очистку перед сдачей:

### Шаг 1: Проверь и удали ошибочно созданные файлы

Проверь `git status` на наличие мусорных файлов, созданных по ошибке:

```bash
git status --porcelain | grep "^??"
```

**Типичные признаки мусорных файлов:**
- Файлы с пробелами/спецсимволами в имени: `"Exit code: "`, `"some output"`
- Файлы с именами bash-команд: `echo`, `cat`, `grep`, `test`, `true`, `false`
- Файлы с цифрами или случайными именами: `1`, `123`, `output`
- Пустые файлы без расширения в неожиданных местах

**Примеры мусора (удалить):**
```bash
rm -f "Exit code: " echo cat grep test true false
rm -f 1 2 3 output result tmp temp
```

**НЕ являются мусором** (легитимные файлы без расширения):
- `README`, `LICENSE`, `Makefile`, `Dockerfile`, `Procfile`
- `.env.example`, `.gitignore`, `.dockerignore`
- Файлы в `.git/`, `node_modules/`, `venv/`

**Если нашёл мусорные файлы — удали их перед коммитом.**

### Шаг 2: Проверь и исправь разрешения изменённых файлов

Используй `git status` для получения списка изменённых файлов:

```bash
# Получи список изменённых/новых файлов
git status --porcelain | awk '{print $2}'

# Проверь разрешения для каждого типа файлов
# Python модули должны быть 644 (rw-rw-r--)
# Shell скрипты должны быть 755 (rwxrwxr-x)
```

Для исправления разрешений:
```bash
chmod 644 path/to/file.py
chmod 755 path/to/script.sh
```

**Стандартные разрешения:**
| Тип файла | Права | Описание |
|-----------|-------|----------|
| `*.py` | 644 | Python модули |
| `*.sh` | 755 | Shell скрипты (исполняемые) |
| `*.html`, `*.css`, `*.js` | 644 | Web assets |
| `*.sql`, `*.yml`, `*.json` | 644 | Конфигурация и данные |
| `*.md` | 644 | Документация |

### Шаг 3: Отметь cleanup как завершённый

**ОБЯЗАТЕЛЬНО** напиши в отчете в фазе 12 фразу **"cleanup completed"** — это маркер для workflow hook.

## Phase 11: Documentation

Обнови документацию если нужно:
- Architecture docs
- README
- CLAUDE.md
- User help/справка

### CHANGELOG.md (ОБЯЗАТЕЛЬНО)

**Добавь запись в CHANGELOG.md** о выполненных изменениях:

```markdown
## [Unreleased]

### Добавлено
- Новая функциональность

### Изменено
- Изменения в существующей функциональности

### Исправлено
- Исправленные баги
```

**Формат записи:**
- Краткое описание изменения (1 строка)
- Ссылка на задачу если есть: `(#N)`
- Группируй по категориям: Добавлено, Изменено, Исправлено, Удалено, Безопасность

**Пример:**
```markdown
### Добавлено
- Фильтры по локациям и подразделениям на странице событий (#6)
- Экспорт посещаемости в Excel с учётом фильтров (#6)

### Исправлено
- Автопереподключение камеры при потере связи (#7)
```

### CLAUDE.md (ОБЯЗАТЕЛЬНО при решении проблем)

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Если во время работы ты столкнулся с проблемами — **ОБЯЗАТЕЛЬНО** запиши решения!

#### Checklist — что ОБЯЗАТЕЛЬНО записать:

| Ситуация | Действие |
|----------|----------|
| Потратил >2 попытки на команду/синтаксис | → Записать правильный синтаксис |
| Нашёл правильный хост/порт методом проб | → Записать рабочий адрес |
| Обнаружил особенность API/библиотеки | → Записать с примером |
| Workaround для ошибки инфраструктуры | → Записать причину и решение |
| Неочевидный порядок действий | → Записать последовательность |

#### Формат записи в CLAUDE.md:

```markdown
## Известные проблемы и решения

### Запуск UI тестов для конкретного класса
**Проблема:** `run_tests.sh ui/test_file::TestClass` не работает
**Решение:** Использовать `-k`: `run_tests.sh ui/test_file.py -k TestClass`

### Подключение Playwright к тестовому серверу
**Проблема:** 127.0.0.1 и host.docker.internal не работают
**Решение:** Использовать `web-test:8080` (имя сервиса в docker-compose)
```

#### Куда записывать:

- **Общие проблемы проекта** → `/workspace/CLAUDE.md`
- **Проблемы конкретного сервиса** → `/workspace/services/<service>/CLAUDE.md`
- **Проблемы тестов** → `/workspace/services/<service>/tests/CLAUDE.md`

**⚠️ НЕ ПРОПУСКАЙ этот шаг!** Следующий разработчик потратит время на те же проблемы, если ты не запишешь решение.

## Phase 12: Complete Task

### Шаг 1: CONFIRMATION PHRASE (ОБЯЗАТЕЛЬНО ПЕРЕД update_task!)

**⚠️ КРИТИЧЕСКИ ВАЖНО: Напиши confirmation phrase ДО update_task!**

Это защита от крашей: если сессия упадёт после update_task но до confirmation,
hook увидит status=done но без подтверждения и заблокирует.

```
I confirm that all task phases are fully completed.
```

**Порядок важен:**
1. ✅ Сначала confirmation phrase (подтверждает что ВСЁ готово)
2. ✅ Потом update_task с status=done

### Шаг 2: Запиши отчёт в задачу

```python
# Подготовь отчёт
report_text = """
### Выполненная работа
- [Что сделано]

### Изменённые файлы
- file1.py — описание
- file2.py — описание

### Тестирование
- Автотесты: [результат]
- Ручное тестирование: [что проверено]

cleanup completed
"""

# Запиши в поле report (НЕ в body!)
update_task(
    project="<project>",
    number=<N>,
    status="done",
    completed="<YYYY-MM-DD>",
    report=report_text  # ← ВАЖНО: поле report, НЕ body!
)
```

**Пользователь сам переведёт задачу в "human approved" после проверки.**

### Шаг 4: Покажи отчёт пользователю и СПРОСИ ПОДТВЕРЖДЕНИЕ

```
✅ ЗАДАЧА ЗАВЕРШЕНА (status: done)

## Отчёт
[Что сделано, какие файлы изменены]

## Изменённые файлы
- file1.py — описание изменений
- file2.py — описание изменений

## Как тестировать (автотесты)
[Команды для запуска автотестов]

## Как тестировать вручную
[Пошаговая инструкция для ручной проверки:
1. Открыть страницу X
2. Войти как пользователь Y
3. Проверить что Z]

Подтвердить? (да/доработать)
- "да" → переведи задачу в "human approved"
- "доработать" → опиши что исправить
```

### Шаг 5: Жди подтверждения

**После подтверждения пользователь сам переводит задачу в "human approved".**

### Шаг 6: Создай коммит

**Используй команду `/commit`** для создания коммита в стиле репозитория:

```
Skill(skill="commit")
```

Команда автоматически:
- Проанализирует стиль коммитов в репозитории
- Добавит изменённые файлы (без .env и credentials)
- Создаст коммит с правильным форматом сообщения
- Добавит Co-Authored-By если используется в репо

## Checklist

### Planning (Phase 0-2)
- [ ] Задача получена из ralph-tasks
- [ ] **Зависимости проанализированы** (явные depends_on + неявные по семантике)
- [ ] Plan Mode выполнен (EnterPlanMode → анализ → ExitPlanMode)
- [ ] **План НЕ содержит номеров строк** (используются устойчивые маркеры)
- [ ] План одобрен пользователем
- [ ] **План записан в поле `plan`** (не в body!)

### Implementation (Phase 3)
- [ ] **CLAUDE.md прочитаны** для затрагиваемых сервисов
- [ ] Implementation выполнен по плану
- [ ] Side issues вынесены в отдельные задачи

### Testing (Phase 4)
- [ ] Unit tests написаны и проходят
- [ ] API tests написаны (если есть endpoints)
- [ ] **UI tests с data-testid** (если есть frontend)
- [ ] **Активное UI тестирование** — формы, кнопки, edge cases
- [ ] Edge cases покрыты тестами
- [ ] Existing tests не сломаны

### Reviews (Phase 5-7)
- [ ] `/pr-review-toolkit:review-pr` выполнен
- [ ] `/security-review` выполнен
- [ ] `/codex-review` выполнен
- [ ] **Все замечания обработаны** с отметками `[main-claude]`
- [ ] Каждое замечание имеет: Fixed / Deferred to #N / Declined

### Testing (Phase 8)
- [ ] Все тесты проходят после исправлений

### Documentation (Phase 11)
- [ ] CHANGELOG.md обновлён
- [ ] **Если были проблемы** → решения записаны в CLAUDE.md
- [ ] **Если >2 попыток на команду** → правильный синтаксис записан

### Finalization (Phase 10, 12)
- [ ] Linters проходят (ruff, djlint)
- [ ] Cleanup выполнен (мусор удалён, разрешения проверены)
- [ ] **Confirmation phrase написана ПЕРЕД update_task**
- [ ] **Report записан в поле `report`** (не в body!)
- [ ] Отчёт показан пользователю
- [ ] Отчёт одобрен пользователем (→ human approved)

## Error Handling

### Критические ошибки (СТОП)

Следующие ситуации требуют **НЕМЕДЛЕННОЙ ОСТАНОВКИ**:

| Ситуация | Действие |
|----------|----------|
| Codex недоступен | СТОП → сообщить → ждать решения |
| Code review не выполнен | СТОП → сообщить → ждать решения |
| Тесты падают после 3 попыток | СТОП → сообщить → ждать решения |
| ralph-tasks недоступен | СТОП → сообщить → ждать решения |

**ЗАПРЕЩЕНО:**
- Заменять недоступный инструмент собственным анализом
- Записывать своё ревью вместо codex/code-review
- Продолжать workflow при критической ошибке
- Игнорировать ошибки "инструмент недоступен"

### Некритические ошибки (можно продолжать)

| Ситуация | Действие |
|----------|----------|
| Замечание от ревью спорное | AskUserQuestion → продолжить |
| Не уверен в реализации | AskUserQuestion → продолжить |
| Документация неполная | Обновить и продолжить |

### Что делать при ошибке

1. **НЕ ПРОДОЛЖАЙ** без решения
2. Сообщи пользователю что именно не работает
3. Предложи варианты:
   - Повторить попытку
   - Пропустить этап (только если пользователь явно разрешит)
   - Остановить workflow
4. **ЖДАТЬ УКАЗАНИЙ** — не принимай решение самостоятельно
